/**
 *  Funcion que cuenta los numeros
 * Autor: Julian Naranjo
 * Martes 12 abril 2024
 */

    function contar(limite){
        for(contador=1;contador<=limite;contador++){
            console.log(contador);
        }
    }
    const contarExp = function(limite){
        for(contador=1;contador<=limite;contador++){
            console.log(contador);
        }
    }